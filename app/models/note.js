/**
 * ! Steps for Creating Schema
 * 1. import Mongoose into a variable
 * 2. import Schema from Mongoose into a variable
 * 3. create a Schema through Schema({}) Constructor
 * 4. save Schema into a variable through mongoose.model()
 * 5. export the Schema variable through module.exports
 */